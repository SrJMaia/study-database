# INT -> Integer
# DECIMAL(M,N) -> M is the total digits, N is the ammount after the separator
# Example: DECIMAL(5,2) = 123.45
# VARCHAR(l) -> String where l is the length
# BLOB -> Binary Large Object, Store large data like images
# DATE -> YYYY-MM-DD
# TIMESTAMP -> YYYY-MM-DD HH:MM:SS